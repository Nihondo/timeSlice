# implementation-todo

## 完了

- [x] `ReportScheduler` による日報の定時自動生成（有効化 + 時刻設定 + AppState連携）

## 未完了

- [ ] `ReportPreviewView`（Markdown プレビュー / クリップボードコピー / Finderで開く）
- [ ] `PromptBuilder` / `ReportGenerator` のユニットテスト追加
